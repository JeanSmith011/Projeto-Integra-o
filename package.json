{
  "name": "grpc-printer-agent",
  "version": "1.0.0",
  "description": "Agente local para imprimir via gRPC",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0",
    "@grpc/proto-loader": "^0.7.10"
  }
}
